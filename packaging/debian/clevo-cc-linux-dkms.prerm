#!/bin/sh
set -e

VERSION=0.1.0
MODULE=clevo-cc

if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
    modprobe -r "$MODULE" 2>/dev/null || true
    command -v dkms >/dev/null 2>&1 && dkms remove -m "$MODULE" -v "$VERSION" --all 2>/dev/null || true
fi

#DEBHELPER#
